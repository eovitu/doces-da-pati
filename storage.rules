rules_version = '2';

// Fotos dos produtos: leitura pública, upload só de quem tem um documento
// em admins/{uid} no Firestore (mesmo critério do firestore.rules).
service firebase.storage {
  match /b/{bucket}/o {
    match /produtos/{allPaths=**} {
      allow read: if true;
      allow write: if request.auth != null
                   && firestore.exists(/databases/(default)/documents/admins/$(request.auth.uid))
                   && request.resource.size < 5 * 1024 * 1024 // máx 5MB por foto
                   && request.resource.contentType.matches('image/.*');
    }
  }
}
